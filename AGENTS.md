# AGENTS.md

This repository is empty and has no established patterns yet.

- No build, test, lint, or dev commands exist.
- No framework, language, or toolchain is configured.
- No CI, code style, or workflow conventions are in place.
- Treat everything as greenfield until evidence of prior patterns emerges.

When you introduce tooling, commit config files (e.g. `package.json`, `tsconfig.json`, `.github/workflows/`) and let them serve as executable truth. Update this file only when you discover conventions that would not be obvious from the config alone.
